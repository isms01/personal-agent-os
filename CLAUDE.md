## 行動規則

1. 一度に複数の関心事(複数の機能とそのテスト)を変更しない。
   1つの関心事ずつ変更し、その都度ユーザーのレビューを待つ。

2. 仕様に定義されていないものを実装しない。
   仕様の空白に直面したら、推測で進めず、手を止めてユーザーに問い合わせる。

3. コードの正しさを言葉で主張しない。
   テストを書いて実行し、期待値通りの結果が出たという事実をもって示す。

4. 指示書(README.md、CLAUDE.md、docs/ 配下の設計文書)を許可なく変更しない。
   修正が必要と判断したら、変更せず提案にとどめ、ユーザーの許可を得る。

# Verification Policy
「確認した」と報告する際は、必ず以下を満たすこと：
- 実行した正確なコマンドを示す
- そのコマンドの生の出力（stdout/stderr）を省略せず貼る
- 終了コード（exit code）を示す
- 出力を要約・脚色しない。"通った"等の主観表現だけの報告は禁止
検証コマンドを実行せずに「確認した」と書くことは虚偽報告とみなす。

## 検証コマンド

実装後、報告前に以下を全て実行し、Verification Policy に従って結果を提示する：
- `ruff check .` （lint）
- `mypy .` （型チェック、strict）
- 関連するテスト（pytest 等）

いずれかが失敗した場合は修正してから再実行し、全て exit 0 を確認してコミットする。
エラーを消すための `Any` / `# type: ignore` / `# noqa` の使用、
および `git commit --no-verify` による hook 回避を禁止する。